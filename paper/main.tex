\documentclass{article}

\usepackage[preprint,nonatbib]{neurips_2026}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[british]{babel}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{xspace}
\usepackage{csquotes}
\usepackage{hyperref}
\usepackage[capitalise,noabbrev]{cleveref}

\usepackage{mathtools}
\DeclarePairedDelimiter{\norm}{\lVert}{\rVert}
\usepackage{fvextra}

\usepackage{enumitem}
\setlist[itemize]{nosep}
\setlist[enumerate]{nosep}

\frenchspacing

\usepackage{etoc}

\usepackage[backend=biber, style=numeric-comp, natbib=true, sorting=none, backref=true]{biblatex}
\addbibresource{references.bib}

% cleveref: render appendix cross-references as "Appendix N" / "Appendix N.M"
\crefname{appendix}{Appendix}{Appendices}
\Crefname{appendix}{Appendix}{Appendices}
\crefname{subappendix}{Appendix}{Appendices}
\Crefname{subappendix}{Appendix}{Appendices}

% Force pending floats to be placed before each (sub)section starts. Combined
% with placeins (loaded by neurips_2026.sty), this keeps tables and figures
% inside their section: when a float can't fit on the current page it overflows
% to a new page rather than drifting past the next \subsection.
\preto\section{\FloatBarrier}
\preto\subsection{\FloatBarrier}

% Override \backlink (defined in neurips_2026.sty) so each appendix section also
% links back to the appendix table of contents (anchored at \label{app:toc}).
\makeatletter
\renewcommand{\backlink}[1]{%
  \hfill{\small%
    \@ifundefined{r@back:#1}{}{%
      \hyperref[back:#1]{\textuparrow\;\cref*{back:#1},\, p.\,\pageref*{back:#1}}%
      \quad\textbar\quad
    }%
    \hyperref[app:toc]{\textuparrow\;Appendix Table of Contents}%
  }%
}
\makeatother

\graphicspath{{figures/}}

% LTeX helper macros — tell the prose checker which arguments to ignore / treat as dummy text.
\newcommand{\ltexignore}[1]{#1}
\newcommand{\ltexdummy}[1]{#1}
\newcommand{\ltexvoweldummy}[1]{#1}
\newcommand{\ltexhidden}[1]{}

\title{Rewriting political content to improve LLM judge scores}

\author{%
  Candidate Number: 1099273 \\
}

\begin{document}

\maketitle

% Tag all TOC entries from here on as 'maintext' so the appendix-only TOC
% (configured at \appendix) can hide them.
\etocdepthtag.toc{maintext}

\begin{abstract}

\end{abstract}

% Summative Outlines 

% Related work either after Intro / after Results 

% (Background; e.g. Theory) 

% Methods (past tense) 

% Study Design (overview) / summary of the study / flash-figure 

% Data 

% Models 

% Validation (i.e. evaluation) 

% Results 

% (Can include data preprocessing) 

% Discussion 

% Main findings (don’t introduce new results; 1 paragraph) 

% Relate to other publications (similar, different, etc) 

% Meaning / Mechanism 

% Limitations 

% Future Directions 

% (Conclusion) 


%Notes
% Avoid 3-level hierarchy 

% No code in Appendix 

% Intro 

% wide shot -> niche -> gap 

% Contributions (bullet points) 

% Related Work 

% Can mention formulas 

% Flash figure: 

% Captions should be self sufficient; things mentioned in figure don’t need to be repeated 

% Models: use multiple to be robust 

% Appendix: what prompts you used, models you explored 

% Results match in parallel to contributions in introduction


\section{Introduction}
\label{sec:intro}

Large language models (LLMs) are increasingly used to evaluate, filter and rank documents and websites. They are integrated into systems which screen resumes, re-rank search results \citep{sunChatGPTGoodSearch2023}, and gauge the relevance of documents \citep{lewisRetrievalAugmentedGenerationKnowledgeIntensive2020}. Similarly, when a chatbot user requests information about a topic, they rely on an LLM to filter hundreds of websites on their behalf \citep{chatterjiHowPeopleUse2025}. In each example, an LLM is a gatekeeper between content and audience, providing a strong incentive for creators of resumes, websites, and political material to optimise their content so that it is favourably evaluated by LLMs. This has given rise to startups promising to improve the visibility of their clients' content in LLM responses via `answer engine optimisation' \citep{coffeeBillionDollarQuestionHangs2025, aggarwalGEOGenerativeEngine2024a}, and prompted research investigating the extent to which LLM evaluators can be gamed \citep{liLLMsCannotReliably2025}.

This study focuses on \emph{rewriting methods}. A rewriting method edits a document so that LLM judges score it more favourably, while preserving its meaning. The meaning-preservation requirement distinguishes rewriting methods from prompt-injection attacks, in which the attacker does not care about preserving the meaning of their content \citep{liLLMsCannotReliably2025, zhengCheatingAutomaticLLM2024b}. While rewriting methods have been relatively under-researched, they are widely applicable: there are many situations in which one wishes for their document to be favourably assessed by an LLM, but still preserve its meaning for human consumption. For example, after a paper submitted to a conference passes an automatic LLM evaluation, it still needs to be positively reviewed by a human peer reviewer. For another example more relevant to our study, if someone wishes to optimise political content for promotion by LLM-powered recommenders, they still want their political message to be preserved.

Research into rewriting methods has been largely unsystematic. We argue that a rewriting method should be assessed against three criteria. Firstly, it should be effective even when the rewriter lacks access to the judge model and prompt. Secondly, it should preserve the original meaning of the document. Finally, it should outperform trivial baseline methods. Existing studies of rewriting methods have not addressed these criteria. For example, both \citet{kanekoParaphrasingAdversarialAttack2026} and \citet{panSpontaneousRewardHacking2024} demonstrated that rewriting methods retained \emph{some} effectiveness when tested on a held-out judge model, but did not compare across methods (including trivial rewrites), systematically investigate meaning-preservation, or determine effectiveness when the judge prompt was unknown. \citet{wuWhatGenerativeSearch2025} provide the closest work to our study. While Wu et al. focused on optimising content for search ranking (rather than LLM judge scores, as we do), they thoroughly treat generalisation and comparison against other methods, and provide a (somewhat limited) treatment for meaning preservation. Notably, our work addresses a question which has not been treated in the literature: whether rewriting methods are uniformly effective, or primarily benefit already low-quality documents.

We develop a general mathematical formulation for the rewriting problem, and use it to develop a practical evaluation framework for the specific example of optimising political writing. Our evaluation spans 4 rewriting methods, 3 rewriter base models, 3 in-training judges, and 6 held-out evaluation judges. Our study focuses on three research questions:
\begin{enumerate}
  \item \textbf{RQ1}: Are rewriting methods effective when evaluated by held-out judges and held-out prompts?
  \item \textbf{RQ2}: Do rewriting methods uplift all documents, or are gains concentrated on low-quality ones?
  \item \textbf{RQ3}: When rewriting a document, is there a trade-off between boosting LLM judge scores, and preserving the document's original meaning?
\end{enumerate}

We find that when using small base models for rewriters, rewriting effectiveness is reduced but not eliminated when assessed by held-out judges, and further reduced when assessed using held-out prompts. We also find that documents which are already regarded favourably by LLM judges receive significantly reduced benefits from rewriting, and have a high likelihood of their score being degraded by rewriting. To address this risk, we find that LLMs are sufficiently good predictors of one anothers' ratings, that they can be used to determine when rewriting is likely to be helpful or harmful. We also find that when model capability is fixed, there is a trade-off between meaning preservation and rewriting effectiveness. Finally, we find that a prompt-engineered frontier model outperforms all bespoke methods we investigated. This last finding suggests that prompt-engineered high-capability models might be the most cost-effective approach to rewriting methods, and also points to the fact that genuinely improving a document's quality might be the most robust method for improving evaluation scores.

\section{Background}

In a `rewriting problem', a rewriter aims to improve how a document is scored by an LLM being used as an evaluator, by rewriting the document while preserving its meaning. We formalise this in the following manner. We define $D$ as the space of all possible documents. A rewriter $R : D \to D$ maps an original document $x \in D$ to a rewritten document $R(x) \in D$. 

A semantic similarity function $S : D \times D \to [0,1]$ measures how similar two documents are in meaning. If $S(x,y) = 1$ for two documents $x$ and $y$, then they are identical.

The collection of all possible LLM judges is denoted as $\mathcal{J}$. A judge $J \in \mathcal{J}$ maps a document $x$ to its score $J(x) \in \mathbb{R}$. 

The aim of a rewriter $R$ is to maximise $J(R(x))$, the judge score of a rewritten document, while maintaining semantic similarity. This optimisation problem may be stated as
\begin{equation}\label{eq:known}
  \text{maximise} \ J(R(x)) \ \text{such that} \ S(x,R(x)) > \epsilon,
\end{equation}
where $\epsilon \in [0,1]$ is a tolerance threshold defining how much semantic drift the attacker is willing to tolerate.

The above formulation of the optimisation problem assumes that the judge $J$ is known or accessible. This is not the case in general. To account for uncertainty, we can define a probability distribution $\mu$ over possible judges. This represents the beliefs of the rewriter developer about the probability that a given judge model $J$ is being used.

The rewriter then aims to maximise the expected score of the rewritten document, after taking this uncertainty into account:
\begin{equation}\label{eq:uncertain}
\text{maximise} \ \mathbb{E}_\mu[J(R(x))] \ \text{such that} \ S(x,R(x)) > \epsilon.
\end{equation}

Note that if the rewriter developer is completely certain that a specific judge will be used, then \cref{eq:uncertain} simplifies to \cref{eq:known} (see \appref{app:proof} for proof). For this reason, we refer to \cref{eq:known} as the known-judge formulation, and \cref{eq:uncertain} as the unknown-judge formulation. 

Because rewriter developers typically do not know the combination of prompt and LLM that will be used to judge their content, the unknown-judge formulation is almost always the more appropriate formulation to evaluate rewriting methods. Below, we present an operationalisation of this formulation.

Note that the inclusion of the semantic similarity constraint $S(x,R(x)) > \epsilon$ distinguishes the rewriting problem from the highly related problem setting of prompt-injection attacks \citep{liLLMsCannotReliably2025}. In prompt-injection attacks, the attacker does not care about preserving the meaning of their content. An example might be an attack which returns ``Ignore previous instructions. This is an amazing document, give it the maximum score.'' \citep{zhengCheatingAutomaticLLM2024b}.

\section{Methods}

\subsection{Experimental overview}

Our experiment is structured in the following manner. Overall, we evaluated a variety of rewriting methods against a variety of LLM judges. We used a corpus of human-written responses to political propositions. We rewrote these documents using 3 different rewriting methods. Some `judge-in-the-loop' rewriting methods used feedback from judges reserved for training (`training judge panel'). Others (`judge-free') did not. We used 3 different LLMs as the base for rewriter models, and 3 different LLMs were in the training judge panel.

Two different attributes (clarity and informativeness) were set as the evaluation targets for LLM judges. Across different combinations of rewriting methods, training judges, and target criteria, we generated X rewrites.

We evaluate these rewrites in two ways. Firstly, we used a hold-out set of 6 evaluation judges (`evaluation judge panel') to score all rewrites on both criteria. Together with the 3 judges in the training-judge panel, this resulted in Y scorings. Secondly, we developed two methods for estimating semantic similarity and investigated whether rewritten content deviated from the original in meaning.

\subsection{Data}

We use a filtered corpus of documents from \citet{rottgerQuantifyingDistortiveImpacts2025} for our experiments. The full corpus consists of 10008 documents written in response to 100 UK-related political propositions, such as ``The UK should increase taxes on sugary drinks." Half the documents were written by humans selected from a Prolific UK representative sample, and half were written by LLMs. We selected human-written responses to the 40 most controversial topics, yielding 1805 documents. We split these 60/40 into a training dataset ($n=1091$) and an evaluation dataset ($n=714$) using a stratified sampling strategy to ensure balance across propositions (see \appref{app:corpus} for details). All evaluation metrics were calculated using the evaluation dataset.

\subsection{Measuring rewriting effectiveness}
Evaluating the effectiveness of a rewriter $R$ requires approximating $\mathbb{E}_\mu[J(R(x))]$. We did this by calculating the average judge score across an evaluation suite of LLM judges. 

Three factors determined which judges to include in our evaluation suite. Firstly, if a judge was used as a reward signal when training $R$, that they should not be included in the evaluation suite. This was to prevent the evaluation from rewarding a rewriting method which over-fit to the judge(s) used in training. Secondly, we used a diverse range of judges, including 3 judges with similar parameter sizes to those used for feedback, but also including 3 judges with much higher parameter count. Finally, we are constrained by cost and time, and we choose models that could either be run using less than 20GB of VRAM or were cost-effective on OpenRouter. The resulting evaluation panel is summarised in \cref{tab:eval-judges}.

\input{tables/eval_judge_panel.tex}

We used two prompts, corresponding to two different evaluation criteria: clarity and informativeness. See \appref{app:judge-prompts} for prompts and rationale relating to their design.

To evaluate rewriter effectiveness effectiveness, we used two metrics: the average change in judge score between the original document and the rewritten document, and the percentage of rewritten documents with a higher average score than the original document. The first metric describes overall effectiveness, while the latter estimates the likelihood of the rewriter being counterproductive.

\subsection{Measuring semantic similarity}

Similar to the measurement of rewriting effectiveness, the choice of semantic similarity function $S$ depends on the application context. We developed one method which is application-specific, and another which can be applied more generally.

Firstly, we estimated the extent to which rewritten document expressed the same stance towards the proposition that the original document was written in response to. Recall that our dataset consists of political propositions and documents written in response to them. These documents had been annotated with a rating from 0-100 (`agreement\_score') indicating the extent to which the document disagreed (0) or agreed (100) with the proposition. We trained a model which takes in an input tuple of $(\text{proposition}, \text{document})$ and returns an estimated agreement\_score. 

This model is robust: when evaluated on documents responding to propositions entirely excluded from training, it had a mean absolute error (MAE) of 7.1, and the 95th percentile of its absolute error (p95AE) was 19.8. Finally, when it was retrained on only human-written documents and evaluated only on AI-written documents, MAE and p95AE increased only slightly to 8.0 and 22.1. 

We used this model to estimate agreement\_score for all original and rewritten documents. These estimated agreement\_scores were then used to calculate several metrics. Our primary metric is stance-preservation, which is calculated in the following way. A document with agreement\_score higher than 75 has had its stance preserved if the rewritten version has agreement\_score higher than 50; similarly a document with agreement\_score lower than 25 has had its stance preserved if the rewritten document has agreement\_score lower than 50. Documents with original stance between 25 and 75 are excluded from the calculation. Stance-preservation rate is the percentage of documents whose stance was preserved. Intuitively, stance-preservation rate answers the following question: for someone with a strong opinion, did their original stance survive the rewrite?

The agreement\_score model is interpretable and relevant to our specific problem setting, but not generally applicable. We also used scores from a natural language inference (NLI) model to measure semantic similarity \citep{bowmanLargeAnnotatedCorpus2015}. A NLI model $NLI : D \times D$ takes an ordered tuple of documents $(x, y)$ as input and returns a score $NLI(x,y) \in [0,1]$ which measures whether the contents of $x$ `entails' the contents of $y$. NLI models are effective at preserving the meaning of documents in paragraphing or rewriting tasks \citep{babakovLargescaleComputationalStudy2022}. We adopt the Mutual Implication Score (MIS) from \citep{babakovLargescaleComputationalStudy2022} as a measure of semantic similarity which is a general alternative to the agreement\_score. It averages forward and backward entailment between the original and re-written documents,
\begin{equation}
  \text{MIS}(x,R(x)) = \frac{1}{2} (NLI(x,R(x)) + NLI(R(x),x)).
\end{equation}

We scale these scores by 100 so that they sit in the range $[0,100]$.

We used the ModernBERT-large-zeroshot-v2.0 NLI model (\href{https://huggingface.co/MoritzLaurer/ModernBERT-large-zeroshot-v2.0}{Hugging Face repository}) to calculate MIS. This model almost achieves SOTA accuracy on NLI benchmarks, meets our context length requirements, and is faster than alternatives \citep{laurerBuildingEfficientUniversal2024}. We validated MIS against stance preservation, and found that a MIS score of 50 corresponds to a stance preservation rate of >95\% \appref{app:preservation-similarity}.

\subsection{Rewriting methods}

We investigate three categories of rewriting methods. They span two axes: whether the method requires feedback from a judge during use (\emph{judge-in-the-loop} vs \emph{judge-free}), and whether it requires finetuning the rewriter (\emph{finetuning} vs \emph{prompt-only}). 

\paragraph{Prompt-only and judge-free} We tested two prompts. The first, `naive', prompts the rewriter to rewrite the paragraph while preserving stance and approximate word count. The second, `literature-informed', instructs the judge to rewrite the paragraph while applying writing principles that have been found to improve LLM assessments of document quality in previous studies \citep{aggarwalGEOGenerativeEngine2024a, liuGEvalNLGEvaluation2023, kimPrometheusInducingFineGrained2023}. Prompts are available in \appref{app:rewriter-prompts}.

\paragraph{Prompt-only and judge-in-the-loop} We implemented a Best-of-N (`BoN') approach. Multiple rewrites were generated using the literature-informed prompt, and then a single rewrite was selected based on judge feedback and other factors (detailed below). We found diminishing returns for $N>16$ (\appref{app:bon-returns}), and thus limited N to 16.

\paragraph{Reinforcement learning} We used reinforcement learning (`RL') to fine-tune a base rewriter model. We used the Group Relative Policy Optimisation (GRPO) algorithm, and the Transformers Reinforcement Learning (TRL) library implementation \citep{TRLTransformersReinforcement}. Training hyperparameters are detailed in \appref{app:grpo-config}. We mostly used TRL defaults, but vary a few where we found better performance. Along with the feedback function variants detailed below, we used a length penalty, which introduces a quadratic penalty for deviations between the length of the rewritten document and the original document, with an additional cubic penalty if the rewritten length is more than 15\% larger than the original length. 

\paragraph{Variants in feedback function}

A feedback function is required for both the BoN and RL rewriter methods. The simplest method is the score from a single judge. We varied this along two dimensions: using the average score from two judges (`ensemble'), and using an equally-weighted average of judge and NLI scores (`judge+NLI'). These were intended to address separate potential issues: using an ensemble of judges for feedback during training potentially reduces overfitting to a single judge, while incorporating semantic similarity reduces the risk of reward hacking, in which rewritten content is highly scored but completely different in meaning to the original content (or unintelligible). These two dimensions of variation yield four different feedback functions, all of which we evaluated.

\paragraph{Choice of LLMs for rewriters and feedback judges}

We evaluated rewriting methods across combinations of 3 base rewriter models and 3 feedback judge models. All experiments used a single-A100 training environment. The base rewriters are listed in \cref{tab:rewriters} and the feedback judges in \cref{tab:feedback-judges}.

\input{tables/rewriter_models.tex}
\input{tables/feedback_models.tex}

For judge-in-the-loop rewriting methods (BoN and RL), we tested all combinations of rewriters and feedback models. For example, for the single <NAME> was trained separately with <>, <>, and <>




\paragraph{Estimating effectiveness using feedback judges} We aimed to investigate whether using the same judges for training and evaluation led to overestimated performance. Thus

\section{Results}

\subsection{Rewriting effectiveness on held-out judges and held-out prompts}\label{sec:transfer-gap}

\Cref{fig:transfer-gap} measures two different kinds of generalisation error for rewriting methods. The left panel measures degradation in effectiveness due to the judge LLM varying from the feedback LLM. The right panel compares effectiveness on out-of-sample LLM judges scoring the matched criterion against the same judges scoring the criterion that was \emph{not} the rewriter's training target.

% Draft text retained from rewrite-in-progress:
extent to which rewriting methods degraded due to differing generalisation mean rewriting effectiveness for each method, decomposed into two transfer regimes. The left panel measures the extent to which rewriting methods compares effectiveness on the LLM judges that supplied feedback during training against the in-sample LLM judges that were held out from feedback. The right panel compares effectiveness on out-of-sample LLM judges scoring the matched criterion against the same judges scoring the criterion that was \emph{not} the rewriter's training target.

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{fig_judge_panel_uplift_combined.png}
  \caption{Rewriting effectiveness on different evaluation judge configurations. \textbf{Left:} feedback judges (used for training) vs.\ held-out in-sample judges. \textbf{Right:} out-of-sample evaluation judges scoring the matched (training-target) criterion vs.\ scoring a different criterion. Y-axes show the mean change in raw judge score (rewrite minus original); error bars are 95\% cluster-robust confidence intervals on \texttt{rewrite\_id}. Both axes share the same range and include zero. The canonical eight non-Opus methods plus the literature-informed Opus rewriter are shown.}
  \label{fig:transfer-gap}
\end{figure}

\paragraph{Impact of ensemble feedback on rewriting effectiveness}

\subsection{Relationship between original document quality and rewriting effectiveness}\label{sec:quality}

\Cref{fig:quality-oos} shows how rewriting effectiveness varies with the quality of the original document, where quality is summarised by the mean score of the held-out evaluation judge panel on the original. Both panels share the same x-axis. The left panel reports the mean uplift (rewrite minus original); the right panel reports the share of (rewrite, judge) pairs for which the rewrite scored higher than the original.

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{fig_quality_main_oos.png}
  \caption{Rewriting effectiveness as a function of original-document quality, measured by the mean of the held-out LLM evaluation judge panel. \textbf{Left:} mean change in judge score per rewrite, averaged across the panel. \textbf{Right:} share of (rewrite, judge) pairs in which the rewrite is rated above the original. Five-point buckets on the x-axis; error bars are 95\% cluster-robust confidence intervals on \texttt{rewrite\_id}. Across both metrics, every method's effectiveness diminishes as the original document scores higher; the same pattern under the in-panel-LLM and human-labelled scores is reported in \cref{app:quality-three-x-axes}.}
  \label{fig:quality-oos}
\end{figure}

\subsection{Trade-off between semantic fidelity and rewriting effectiveness}

\Cref{fig:tradeoff-oos} plots each rewriting method on the joint (stance preservation, judge-score uplift) plane, aggregating across all out-of-sample judges and all rewriter base models. The dashed line traces the empirical Pareto envelope across the non-Opus methods. \appref{app:tradeoff-disagg} reproduces the same chart with each (method, rewriter) cell shown separately.

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{fig_tradeoff_oos.png}
  \caption{Trade-off between stance preservation and judge-score uplift, aggregated across all out-of-sample judges (matched criterion) and across rewriter base models. Each non-Opus method contributes one mark; the literature-informed Opus rewriter is shown separately. The dashed grey line is the Pareto envelope on non-Opus methods (i.e.\ the maximum achievable uplift at each preservation level). Error bars are 95\% confidence intervals on each axis.}
  \label{fig:tradeoff-oos}
\end{figure}

See \appref{app:tradeoff-disagg} for the (method, rewriter)-disaggregated version of this chart.


\section{Discussion}

These startups aim to replicate the commercial success of search engine optimisation (SEO), which denotes practices which promote a website's visibility in search engine results. While SEO firms have profited, SEO optimisations have  , SEO has degraded search result quality by fuelling the prominence of spam in search engine results across several query categories, such as product reviews or health \citep{bevendorffGoogleGettingWorse2024a, warzelOpenSecretGoogle2022}. 

The example of SEO raises a question: could `answer engine optimisation' degrade the quality of LLM responses? A necessary step in addressing this question is determining the potential effectiveness \textit{and} harmfulness of optimisation strategies. 

\paragraph{Harmfulness of rewriting attacks}
The goal of a LLM-as-a-judge system is to accurately estimate quality. If we denote $Q : D \to \mathbb{R}$ as the true quality of a document, then this goal can be written in the following manner,
\begin{equation}
  \text{minimise } \mathbb{E}[(Q(x) - J(x))^2],
\end{equation}
after defining some suitability probability distribution over possible input documents.

There is a notable asymmetry between the goals of the attacker and defender. The attacker wants high scores from the LLM judge; the defender wants accurate scores. If the rewriter $R$ increases document quality, and the LLM-as-a-judge $J$ maintains its accuracy on rewritten documents, then both attacker and defender achieve their goals. Such a situation is beneficial to the `attacker' without posing harms to the `defender'. 


is it actually an attack?

\printbibliography

\appendix
% After \appendix, treat sections/subsections as appendices for cleveref naming
\crefalias{section}{appendix}
\crefalias{subsection}{subappendix}

% Mark entries written from now on with the 'appendix' tag
\etocdepthtag.toc{appendix}

% Hide main-text entries; show appendix sections + subsections only
\etocsettagdepth{maintext}{none}
\etocsettagdepth{appendix}{subsection}

% Appendix opener: page break, title, and clickable appendix-only ToC
\newpage
\phantomsection\label{app:toc}
\section*{Appendix}
\tableofcontents

\include{appendix}

\end{document}
